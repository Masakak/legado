# 朗读功能用户使用指南

本文说明文本阅读中的朗读功能，包含系统 TTS、HTTP TTS、朗读控制、定时、耳机按键、HTTP TTS 音频缓存和删除书籍后的缓存清理。本文不包含有声书播放功能。

## 1. 功能概览

朗读功能会把当前书籍正文转换为语音播放。它支持两类朗读引擎：

- 系统 TTS：使用 Android 系统或已安装的本地 TTS 引擎。
- HTTP TTS：使用用户配置的在线 TTS 规则，通过网络生成音频。

朗读过程中可以：

- 从当前阅读位置开始朗读。
- 暂停、继续、停止朗读。
- 切换上一段、下一段、上一章、下一章。
- 调整朗读语速。
- 设置定时停止。
- 从选中文本开始朗读，或只朗读选中文本。
- 在通知栏、锁屏媒体控件和耳机按键中控制朗读。
- 使用 HTTP TTS 时缓存章节音频，离线或重复朗读时复用缓存。

## 2. 朗读前准备

### 2.1 选择朗读引擎

在阅读页打开朗读面板，进入“设置” > “朗读引擎”。

可选择：

- 系统默认：使用系统当前默认 TTS。
- 系统 TTS 引擎：使用系统中已安装的某个 TTS 引擎。
- HTTP TTS：使用应用内配置或导入的在线 TTS 规则。

朗读引擎可以保存为：

- 本书：只对当前书籍生效。
- 通用：作为全局默认朗读引擎。

如果本书单独设置了朗读引擎，会优先使用本书设置；否则使用通用设置。

### 2.2 系统 TTS 准备

使用系统 TTS 时，需要系统中安装可用的 TTS 引擎，并在系统 TTS 设置里选择合适的语言和声音。

如果朗读失败、无声音或提示 TTS 初始化失败，请先打开系统 TTS 设置检查：

- 是否安装了 TTS 引擎。
- 是否下载了对应语言的语音包。
- 系统默认 TTS 是否能正常试听。

### 2.3 HTTP TTS 准备

使用 HTTP TTS 时，需要先在朗读引擎页面添加或导入 HTTP TTS 规则。

HTTP TTS 规则本质上是一个 URL 请求规则，规则中可以使用：

- `speakText`：当前要朗读的文本。
- `speakSpeed`：朗读速度参数。

HTTP TTS 依赖网络和服务端可用性。如果服务端返回非音频内容、登录状态失效、网络超时或被限流，朗读可能暂停或生成失败。

## 3. 开始朗读

### 3.1 从阅读页开始

在阅读页打开底部菜单，点击“朗读”。

首次点击会启动朗读服务，并从当前阅读位置开始朗读。朗读启动后会显示前台通知，通知中显示书名、章节和播放状态。

再次点击“朗读”：

- 正在朗读时：暂停。
- 已暂停时：继续。
- 未运行时：重新启动朗读。

### 3.2 从滚动阅读位置开始

如果当前使用滚动阅读模式，朗读会尽量从屏幕中当前可见位置开始，而不是总从页面开头开始。

### 3.3 从选中文本开始

在正文中选中文字后，选择菜单里有“朗读”。

“朗读”有两种模式：

- 朗读选择内容：只读当前选中的文字。
- 从选择的地方开始一直朗读：从选中位置开始进入全文朗读。

在文本选择菜单中长按“朗读”，可以在这两种模式之间切换。

注意：全文朗读正在运行时，不能再同时朗读选中文字，需要先暂停或停止全文朗读。

## 4. 朗读控制

### 4.1 朗读面板

朗读运行后，再次打开朗读面板，可以使用以下控制：

- 上一章：切到上一章并继续朗读。
- 上一段：回到上一段。
- 播放/暂停：继续或暂停当前朗读。
- 停止：结束朗读服务。
- 下一段：跳到下一段。
- 下一章：切到下一章并继续朗读。
- 目录：打开章节列表。
- 主菜单：回到阅读主菜单。
- 后台：关闭阅读界面，让朗读在后台继续。
- 设置：打开朗读设置。

### 4.2 通知栏和锁屏控制

朗读运行时会显示通知。通知中可控制：

- 上一章。
- 暂停或继续。
- 停止。
- 下一章。
- 增加定时时间。

如果开启系统媒体控制兼容，锁屏和系统媒体控件也可以控制朗读。

### 4.3 耳机按键和媒体键

如果开启“耳机按键启动朗读”，可以通过耳机或媒体按键启动朗读。

朗读运行中：

- 播放/暂停键：暂停或继续。
- 上一首/下一首键：默认控制上一段/下一段。
- 如果开启“媒体键上一首/下一首按章节切换”，上一首/下一首会改为上一章/下一章。

如果开启“退出应用后媒体键仍可启动”，在应用不在前台时也可以通过媒体键尝试启动最近阅读书籍的朗读。

## 5. 语速和定时

### 5.1 语速

朗读面板中可以调整朗读语速。

“跟随系统”开启时：

- 系统 TTS 使用系统 TTS 自己的语速设置。
- HTTP TTS 使用默认速度参数。

“跟随系统”关闭时：

- 可以用滑条或加减按钮设置应用内语速。
- 显示值按倍率展示，例如 `1.0` 表示正常速度。

调整语速后，正在朗读的内容会暂停并恢复，使新语速尽快生效。

### 5.2 定时停止

朗读面板中可以设置定时停止。

支持：

- 使用滑条设置 0 到 180 分钟。
- 点击时间文字，从常用时间中选择：0、5、10、15、30、60、90、180 分钟。
- 点击通知中的定时按钮，每次增加 10 分钟，最多 180 分钟，再次增加会回到 0。

倒计时只在朗读未暂停时递减。倒计时到 0 后，朗读服务会自动停止。

## 6. 朗读设置

朗读设置在朗读面板的“设置”中。

### 6.1 忽略音频焦点

关闭时，朗读会遵守系统音频焦点：

- 其他应用长期占用音频焦点时，朗读会暂停。
- 短暂失去焦点后，如果之前正在朗读，获得焦点时会继续。

开启时，朗读不处理系统音频焦点，可能与其他音频同时播放。

### 6.2 来电期间暂停朗读

此选项只有在“忽略音频焦点”开启时可用。

开启后，来电响铃时会暂停朗读，来电结束后如果之前正在朗读，会继续朗读。该功能需要读取手机状态权限。

### 6.3 朗读服务唤醒锁

开启后，朗读服务运行时会尝试持有唤醒锁和 Wi-Fi 锁，降低后台休眠导致朗读中断的概率。

部分设备开启唤醒锁后反而可能更容易被系统限制后台。如果开启后后台表现变差，可以关闭。

### 6.4 系统媒体控制兼容

用于改善部分 Android 版本或设备上的锁屏媒体控制兼容性。开启后，系统媒体控件可显示朗读状态和控制按钮。

### 6.5 媒体键上一首/下一首行为

默认情况下，媒体键上一首/下一首控制上一段和下一段。

开启后，媒体键上一首/下一首改为控制上一章和下一章。

### 6.6 按页朗读

关闭时，朗读按章节正文段落组织。

开启时，朗读按当前分页结果组织，更贴近当前页面显示。切换该设置时，如果朗读正在运行，会触发朗读状态刷新。

### 6.7 流式播放音频

该选项仅对 HTTP TTS 有效。

关闭时：

- 应用会优先生成并保存当前段落或章节音频文件。
- 已缓存音频会直接复用。
- 稳定性更好，但首次等待可能更长。

开启时：

- 应用会边下载边播放。
- 网络较差时可能断续。
- 播放前仍会优先检查已有持久缓存，已缓存段落会直接播放本地文件。

## 7. HTTP TTS 缓存

HTTP TTS 缓存用于保存在线 TTS 生成的音频文件。缓存命中后，再次朗读同一段内容时可以直接播放本地音频，减少等待和网络请求。

### 7.1 自动缓存

正常使用 HTTP TTS 朗读时，应用会自动缓存已生成的段落音频。

朗读当前章节时：

- 如果段落音频已经存在，直接播放缓存。
- 如果段落音频不存在，请求 HTTP TTS 并写入缓存。

当前章节处理完成后，应用还会尝试预缓存下一章前面若干段，用于减少切章后的等待。

### 7.2 手动缓存章节

在章节列表中长按章节，可以看到 HTTP TTS 缓存菜单：

- 缓存本章 HTTP TTS。
- 缓存后续 10 章 HTTP TTS。
- 缓存后续 100 章 HTTP TTS。
- 缓存后续所有章节 HTTP TTS。
- 删除本章 HTTP TTS 缓存。
- 清空全部 HTTP TTS 缓存。

只有当前朗读引擎是 HTTP TTS 时，才能加入 HTTP TTS 缓存队列。

章节列表中已完整缓存的章节会显示缓存状态图标。

### 7.3 后台缓存队列

手动缓存会启动后台 HTTP TTS 音频缓存服务，并显示前台通知。

队列规则：

- 章节队列串行执行：先缓存第 1 章，再缓存第 2 章。
- 单章内部段落有限并行：同一章内可以同时缓存多个段落。
- 段落并发数来自全局线程数设置，但限制在 1 到 4 之间。

并发数换算：

- 全局线程数为 1：HTTP TTS 段落缓存串行。
- 全局线程数为 2：最多 2 个段落同时请求。
- 全局线程数为 3：最多 3 个段落同时请求。
- 全局线程数为 4 或更高：最多 4 个段落同时请求。

这样可以提高缓存速度，同时降低 HTTP TTS 服务端限流或超时的概率。

### 7.4 暂停或停止后台缓存

后台缓存通知中有取消按钮。点击取消会停止当前缓存服务。

停止后：

- 已经写入成功的段落缓存会保留。
- 未完成的段落不会标记为完整章节缓存。
- 下次再次缓存同一章节时，会先检测已存在的段落音频，已有段落会跳过，只继续补齐缺失段落。

### 7.5 正在朗读时又手动缓存同一章节

如果正常朗读时已经自动缓存了某些段落，再手动缓存同一章或下一章：

- 后台缓存任务会检查本地文件。
- 已经存在的段落不会重复下载。
- 缺失的段落会继续加入下载。
- 章节只有在所有需要朗读的段落都存在后，才会显示为完整缓存。

### 7.6 缓存命中条件

HTTP TTS 缓存与以下因素相关：

- 书籍。
- 章节。
- 章节标题。
- HTTP TTS 规则 URL。
- 语速。
- 是否按页朗读。
- 章节正文内容。

如果切换了 HTTP TTS 引擎、修改了语速、切换按页朗读、章节内容变化或章节标题处理结果变化，旧缓存可能不再被视为当前章节的完整缓存，需要重新生成。

### 7.7 缓存生命周期

HTTP TTS 音频缓存不会因为朗读服务结束而自动删除。

缓存会在以下情况下删除：

- 用户在章节列表中删除本章 HTTP TTS 缓存。
- 用户在章节列表中清空全部 HTTP TTS 缓存。
- 用户删除书籍时，该书的 HTTP TTS 缓存会同步删除。

朗读过程中生成的静音占位文件不作为长期缓存保存，会在朗读服务销毁后清理。

### 7.8 删除书籍时的缓存清理

删除书籍时，应用会同步删除该书对应的 HTTP TTS 缓存记录和音频文件。

如果某个音频文件仍被其他书籍的缓存记录引用，不会误删。删除发生时，如果后台缓存任务仍在运行，任务在写入前会再次检查书籍是否仍存在，避免删除后重新写入该书缓存。

## 8. 朗读运行生命周期

### 8.1 启动

用户点击朗读后，应用会：

1. 根据本书设置或全局设置选择朗读引擎。
2. 创建前台朗读服务。
3. 读取当前章节排版后的朗读文本。
4. 根据是否“按页朗读”生成朗读段落列表。
5. 请求音频焦点，或在忽略音频焦点时直接播放。
6. 显示朗读通知和媒体会话。

### 8.2 播放中

播放中会持续更新：

- 当前朗读位置。
- 阅读页高亮位置。
- 当前页或章节跳转。
- 通知栏状态。
- 定时倒计时。

朗读到章节末尾后，会自动尝试进入下一章继续朗读。没有下一章时，朗读服务停止。

### 8.3 暂停

以下情况会暂停朗读：

- 用户点击暂停。
- 用户拔出耳机或音频输出变为外放中断。
- 音频焦点长期丢失。
- 开启来电暂停后收到来电。
- HTTP TTS 连续错误达到限制。

暂停时会保存阅读进度，并释放音频焦点。开启唤醒锁时也会释放唤醒锁。

### 8.4 继续

用户点击继续、媒体键播放、通知栏播放，或临时音频焦点恢复后，朗读会从当前记录位置继续。

如果用户暂停后翻页或移动到新的阅读位置，再继续朗读，应用会尽量从新的可见阅读位置开始。

### 8.5 停止

用户点击停止、定时结束、章节末尾无下一章、服务被系统结束时，朗读服务会停止。

停止时会：

- 取消朗读通知。
- 释放媒体会话。
- 放弃音频焦点。
- 保存阅读进度。
- 释放唤醒锁。
- 清理 HTTP TTS 的静音占位文件。

HTTP TTS 正常音频缓存不会因停止朗读而删除。

## 9. 常见问题

### 9.1 没有声音

优先检查：

- 系统媒体音量是否为 0。
- 系统 TTS 是否能在系统设置中试听。
- 当前朗读引擎是否选择正确。
- HTTP TTS 是否需要登录或规则是否失效。
- 是否被其他应用占用音频焦点。

### 9.2 HTTP TTS 朗读断断续续

可以尝试：

- 关闭“流式播放音频”。
- 手动提前缓存章节。
- 降低全局线程数，让 HTTP TTS 缓存并发数降低。
- 检查 HTTP TTS 服务端是否限流。

### 9.3 章节已经缓存但没有显示缓存图标

可能原因：

- 当前引擎、语速或按页朗读设置与缓存时不同。
- 章节正文发生变化。
- 缓存过程中被暂停，章节没有完整缓存。
- 只缓存了下一章前几段，未达到完整章节缓存。

重新对该章节执行“缓存本章 HTTP TTS”即可补齐缺失段落。

### 9.4 删除书籍后缓存还会占空间吗

删除书籍会同步删除该书的 HTTP TTS 缓存。

如果想删除所有书的 HTTP TTS 缓存，可以在任意章节列表长按章节，选择“清空全部 HTTP TTS 缓存”。

### 9.5 调整语速后缓存是否还能命中

语速是 HTTP TTS 缓存命中条件之一。调整语速后，旧语速生成的缓存通常不会作为新语速的完整缓存使用，需要重新生成。

### 9.6 手动缓存可以离线使用吗

如果章节已经完整缓存，并且朗读时仍使用相同 HTTP TTS 引擎、语速、按页朗读设置和相同正文内容，朗读可以直接使用本地缓存音频。

未缓存的段落仍需要网络请求 HTTP TTS。

